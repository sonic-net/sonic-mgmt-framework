#!/usr/bin/make -f  
%:  
	dh $@ --with systemd


override_dh_shlibdeps:
	dh_shlibdeps --dpkg-shlibdeps-params=--ignore-missing-info -l$(shell pwd)/src/cvl/build/libyang/build:$(shell pwd)/src/cvl/build/pcre-8.43/install/lib
