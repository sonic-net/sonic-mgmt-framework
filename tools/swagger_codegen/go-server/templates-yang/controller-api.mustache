{{>partial_header}}
package {{packageName}}

{{#operations}}
import (
	"net/http"

	"rest/server"
){{#operation}}

func {{nickname}}(w http.ResponseWriter, r *http.Request) {
	server.Process(w, r)
}{{/operation}}{{/operations}}
